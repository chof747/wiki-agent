#!/bin/sh
set -eu

branch=$(git branch --show-current)

if [ "$branch" = "main" ]; then
  printf '%s\n' 'Direct commits to main are blocked in this repo. Create or continue an issue worktree branch instead.' >&2
  exit 1
fi
