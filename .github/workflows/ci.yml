name: CI

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.head.ref || github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Compute docs-only change classification
    runs-on: ubuntu-latest
    outputs:
      docs_only: ${{ steps.classify.outputs.docs_only }}

    steps:
      - name: Check out repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Compute docs-only change classification
        id: classify
        run: |
          python -m tools.docs_only_changes from-github-event \
            "${{ github.event_name }}" \
            "${{ github.event_path }}" \
            --github-output "${GITHUB_OUTPUT}"

  pytest:
    name: CI / pytest
    needs: changes
    # Docs-only change sets skip the job entirely, so no runner is provisioned.
    if: needs.changes.outputs.docs_only != 'true'
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Python 3.14
        uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Set up uv
        uses: astral-sh/setup-uv@v8.2.0

      - name: Sync locked dev environment
        run: uv sync --locked --dev

      - name: Run pytest baseline
        run: uv run pytest

  integration:
    name: CI / integration
    needs: changes
    # Docs-only change sets skip the whole job, including service containers.
    if: needs.changes.outputs.docs_only != 'true'
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_USER: integration
          POSTGRES_PASSWORD: integration
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U integration -d postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Check out repository
        uses: actions/checkout@v5

      - name: Set up Python 3.14
        uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Set up uv
        uses: astral-sh/setup-uv@v8.2.0

      - name: Sync locked dev environment
        run: uv sync --locked --dev

      - name: Run harness-backed integration slice
        env:
          WIKI_AGENT_INTEGRATION_ADMIN_DSN: postgresql://integration:integration@127.0.0.1:5432/postgres
          WIKI_AGENT_INTEGRATION_RUNTIME_DSN: postgresql://integration:integration@127.0.0.1:5432/wiki_agent_integration
        run: uv run wiki-agent-integration ci-test

      - name: Dump Wiki-Go harness container on failure
        if: failure()
        run: |
          docker ps -a
          docker logs wiki-agent-integration-harness || true
